# Beyond the Score: Fixed-Budget Benchmarking of Virtual Screening
# Integration Strategies for Decision-Centric Drug Discovery
# Tomarchio, E.G.; Buccheri, R.; Rescifina, A. - Int. J. Mol. Sci.
#
# Tested on Python 3.11
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#
# NOTE: molecular docking (GNINA 1.3.2), protein preparation (YASARA 25.1.13),
# ligand preparation (Open Babel 3.1.1), grid definition (AutoDock Tools 1.5.7)
# and decoy generation (LUDe) run outside this environment and are not
# installable through pip. See the Methods section of the paper.

# --- core -------------------------------------------------------------------
numpy==1.26.4
pandas==2.3.3
# RDKit: required by process_pipeline.ipynb and MCS_analysis.py (fingerprints,
# standardization, MCS, Butina clustering, BEDROC); optional but recommended in
# align_results.py, where it enables SMILES canonicalization before merging.
rdkit>=2024.03.1          # pin: python -c "import rdkit; print(rdkit.__version__)"

# --- ML-QSAR (process_pipeline.ipynb) ---------------------------------------
scikit-learn==1.7.2
joblib==1.4.2

# --- statistics (align_results.py: Friedman omnibus test) -------------------
scipy>=1.11               # pin: python -c "import scipy; print(scipy.__version__)"

# --- optional: uncomment the ones you actually use --------------------------
# matplotlib>=3.8                     # applicability-domain and performance figures
# openpyxl>=3.1                       # reading/writing the .xlsx Supporting Information
# jupyterlab>=4.0                     # to run process_pipeline.ipynb
# chembl-webresource-client>=0.10.9   # programmatic retrieval of ChEMBL bioactivities
